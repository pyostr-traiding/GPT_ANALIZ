version: '3.8'

networks:
  GLOBAL_NETWORK:
    external: true

services:
  panel:
    build: ./
    container_name: gpt-analiz
    command: poetry run python main.py
    env_file: ./.env
    restart: always
    networks:
      - GLOBAL_NETWORK
